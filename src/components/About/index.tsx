import Heading from "../Heading";
import styles from "./about.module.css";

const About = () => {
  return (
    <section>
      <div>
        <Heading level={1} className={styles.title}>Carlos Jimenez</Heading>
      </div>
    </section>
  )
}

export default About;
